# 快捷指令

快捷指令用于在对话中快速触发已定义的操作或切换工作方式。输入 `/` 后，从界面展示的可用指令中选择并按提示补充参数。

不同入口提供的指令可能不同：桌面端对话、IM 机器人和任务页面各自有适用范围。不要把任务中心的创建流程误当作一条普通对话指令；需要正式创建和跟踪的工作，请使用 [任务中心](tasks.md)。

在执行带有文件、工具调用或外部副作用的指令前，确认目标、参数和权限范围。
